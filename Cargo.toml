[package]
name = "cst328"
version = "0.1.0"
edition = "2024"

[dependencies]
embassy-time = { version = "0.5.1", optional = true }
embedded-hal = "1.0"
embedded-hal-async = { version = "1.0", optional = true }
log = "0.4"

[dev-dependencies]
log = "0.4"

[features]
async = ["embedded-hal-async", "embassy-time"]
